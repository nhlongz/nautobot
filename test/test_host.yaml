- name: test hostname
  hosts: all
  gather_facts: false
  tasks:
#------- set hostname---------#
  - name: show ip int brief
    ios_command:
    commands: 
            - show ip int b
    register: interface
  - debug: var=interface.stdout_lines